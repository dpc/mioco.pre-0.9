initSidebarItems({"struct":[["Context","A `Context` provides the capability of saving and restoring the current state of execution.If we have 2 or more `Context` instances, we can thus easily \"freeze\" the current state of execution and explicitely switch to another `Context`. This `Context` is then resumed exactly where it left of and can in turn \"freeze\" and switch to another `Context`.ExamplesSee examples/basic.rs"],["Transfer","This is the return value by `Context::resume()` and `Context::resume_ontop()`."]],"type":[["ContextFn","Functions of this signature are used as the entry point for a new `Context`."],["ResumeOntopFn","Functions of this signature are used as the callback after resuming ontop of a `Context`."]]});